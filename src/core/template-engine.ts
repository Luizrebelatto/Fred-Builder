export async function renderTemplate(template: string, _data?: any): Promise<string> {
  // For MVP, we use TypeScript template strings directly in plugin templates.ts files
  // This function is a placeholder for future EJS integration if needed.
  return template;
}
